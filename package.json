{
  "scripts": {
    "build": "resume export -t jsonresume-theme-paper resume.html && node scripts/build.js && echo '---\nlayout: resume\n---\n' | cat - resume.html > temp && mv temp resume.html && bundle exec jekyll build",
    "start": "bundle exec jekyll serve --config _config_localhost.yml"
  },
  "dependencies": {
    "html-pdf": "^3.0.1",
    "jsonresume-theme-paper": "^0.5.0",
    "jsonresume-theme-paper-plus-plus": "^0.5.1",
    "simple-jekyll-search": "^1.10.0"
  }
}
